#include "holberton.h"

/**
 * print_most_numbers - prints numbers, skipping some
 *
 *return: zero
 */
void print_most_numbers(void)
{
int i = 0;
while (i <= 9)
{
if (i == 2 || i == 4)
{
i++;
continue;
}
_putchar(i);
i++;
}
_putchar('\n');
}
